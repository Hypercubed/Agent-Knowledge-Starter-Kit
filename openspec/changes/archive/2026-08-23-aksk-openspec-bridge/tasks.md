## 1. Schema Scaffolding
- [ ] 1.1 Create `openspec/schemas/aksk-bridge/schema.yaml`.
- [ ] 1.2 Define basic rule patterns for AKSK skill detection in the schema.

## 2. Integration Assets
- [ ] 2.1 Develop `templates/adopters/CLAUDE.md.fragment` to standardize instructions for new project adopters.
- [ ] 2.2 Create `templates/config/rules.yaml` mapping OpenSpec phases to local `task-*` triggers.

## 3. Validation
- [ ] 3.1 Verify schema validation using `openspec schema validate aksk-bridge`.
- [ ] 3.2 Perform a dry-run check of the template insertion into a mock AKSK project folder.
